(function () {
  var rotate, timeline;

  rotate = function () {
    return $('#all-container .stacked-card:first-child')
      .fadeOut(400, 'swing', function () {
        return $('#all-container .stacked-card:first-child')
          .appendTo('#all-container')
          .hide();
      })
      .fadeIn(400, 'swing');
  };

  $('#all-container .next').click(function () {
    return rotate();
  });
}.call(this));

(function () {
  var rotate, timeline;

  rotate = function () {
    return $('#action-container .stacked-card:first-child')
      .fadeOut(400, 'swing', function () {
        return $('#action-container .stacked-card:first-child')
          .appendTo('#action-container')
          .hide();
      })
      .fadeIn(400, 'swing');
  };

  $('#action-container .next').click(function () {
    return rotate();
  });
}.call(this));

(function () {
  var rotate, timeline;

  rotate = function () {
    return $('#adventure-container .stacked-card:first-child')
      .fadeOut(400, 'swing', function () {
        return $('#adventure-container .stacked-card:first-child')
          .appendTo('#adventure-container')
          .hide();
      })
      .fadeIn(400, 'swing');
  };

  $('#adventure-container .next').click(function () {
    return rotate();
  });
}.call(this));

(function () {
  var rotate, timeline;

  rotate = function () {
    return $('#family-container .stacked-card:first-child')
      .fadeOut(400, 'swing', function () {
        return $('#family-container .stacked-card:first-child')
          .appendTo('#family-container')
          .hide();
      })
      .fadeIn(400, 'swing');
  };

  $('#family-container .next').click(function () {
    return rotate();
  });
}.call(this));

(function () {
  var rotate, timeline;

  rotate = function () {
    return $('#shooter-container .stacked-card:first-child')
      .fadeOut(400, 'swing', function () {
        return $('#shooter-container .stacked-card:first-child')
          .appendTo('#shooter-container')
          .hide();
      })
      .fadeIn(400, 'swing');
  };

  $('#shooter-container .next').click(function () {
    return rotate();
  });
}.call(this));

(function () {
  var rotate, timeline;

  rotate = function () {
    return $('#rpg-container .stacked-card:first-child')
      .fadeOut(400, 'swing', function () {
        return $('#rpg-container .stacked-card:first-child')
          .appendTo('#rpg-container')
          .hide();
      })
      .fadeIn(400, 'swing');
  };

  $('#rpg-container .next').click(function () {
    return rotate();
  });
}.call(this));

(function () {
  var rotate, timeline;

  rotate = function () {
    return $('#strategy-container .stacked-card:first-child')
      .fadeOut(400, 'swing', function () {
        return $('#strategy-container .stacked-card:first-child')
          .appendTo('#strategy-container')
          .hide();
      })
      .fadeIn(400, 'swing');
  };

  $('#strategy-container .next').click(function () {
    return rotate();
  });
}.call(this));

(function () {
  var rotate, timeline;

  rotate = function () {
    return $('#sports-container .stacked-card:first-child')
      .fadeOut(400, 'swing', function () {
        return $('#sports-container .stacked-card:first-child')
          .appendTo('#sports-container')
          .hide();
      })
      .fadeIn(400, 'swing');
  };

  $('#sports-container .next').click(function () {
    return rotate();
  });
}.call(this));

(function () {
  var rotate, timeline;

  rotate = function () {
    return $('#racing-container .stacked-card:first-child')
      .fadeOut(400, 'swing', function () {
        return $('#racing-container .stacked-card:first-child')
          .appendTo('#racing-container')
          .hide();
      })
      .fadeIn(400, 'swing');
  };

  $('#racing-container .next').click(function () {
    return rotate();
  });
}.call(this));

(function () {
  var rotate, timeline;

  rotate = function () {
    return $('#fighting-container .stacked-card:first-child')
      .fadeOut(400, 'swing', function () {
        return $('#fighting-container .stacked-card:first-child')
          .appendTo('#fighting-container')
          .hide();
      })
      .fadeIn(400, 'swing');
  };

  $('#fighting-container .next').click(function () {
    return rotate();
  });
}.call(this));

(function () {
  var rotate, timeline;

  rotate = function () {
    return $('#simulator-container .stacked-card:first-child')
      .fadeOut(400, 'swing', function () {
        return $('#simulator-container .stacked-card:first-child')
          .appendTo('#simulator-container')
          .hide();
      })
      .fadeIn(400, 'swing');
  };

  $('#simulator-container .next').click(function () {
    return rotate();
  });
}.call(this));
